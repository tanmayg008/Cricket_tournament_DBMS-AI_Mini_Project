# Copy to .env to configure local development
# Backend base URL (no trailing slash)
VITE_API_BASE_URL=http://localhost:8080

# Enable mock data instead of real backend. Set to "false" to call real API.
VITE_USE_MOCK_DATA=false
